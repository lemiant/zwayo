var api = require('./index')

// =============== The Router =================
// ============================================
module.exports = function(req, res, next, db){
    func = req.url.slice(5).split("?")[0]
    if(typeof api[func] == "function")
        api[func](req, res, next, db)
    else
        next()
}
// ============================================